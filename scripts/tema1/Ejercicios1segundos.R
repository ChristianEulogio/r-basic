#Si hubiéramos empezado a contar segundos a partir de las 12 campanadas 
#que marcan el inicio de 2018, ¿a qué hora de qué día de qué año llegaríamos 
#a los 250 millones de segundos? ¡Cuidado con los años bisiestos!

segundos =250000000
print(segundos)
segundos/60 -> minutos
print(minutos)
minutos/60 -> horas
print(horas)
horas/24 -> dias
dias+2 -> dias
print(dias)

dias/365 -> years
print(years)

dias%/%364 ->full_years
print(full_years)

#ya tenemos los anos

dias%%364 -> dias_sobrantes
print(dias_sobrantes)


dias_enteros_sobrantes=347

0.5185*24

horascompletas=12

0.444*60

minutoscompletos=26

0.64*60

segundoscompletos=38

#2025 el dia 347 a las 12:26:38
13 de diciembre




#Cread una función que os resuelva una ecuación de
#primer grado (de la forma Ax+B=0). Es decir, vosotros
#tendréis que introducir como parámetros los coeficientes
#(en orden) y la función os tiene que devolver 
#la solución. Por ejemplo, si la ecuación es 2x+4=0, 
#vuestra función os tendría que devolver -2.


#Una vez creada la función, utilizadla para
#
#resolver las siguientes ecuaciones de primer grado:
  
# 5x+3=0
#7x+4 = 18
#x+1 = 1

hola <- function(A, B) {
  complex( (A)(x) + (B) = 0 )
}


(3e - π)-> hi

print(hi)

solu = function(A,B){-B/A}

solu(5,3)
solu(7,-14)
solu(1,0)
solu(2,4)


round(3*exp(1)-pi,3)  

3*exp(1)-pi

